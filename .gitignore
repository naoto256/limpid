/target
CLAUDE.md
DESIGN.md
docs/book/
HANDOVER.md

# Local working notes / dev configs — never commit (per global CLAUDE.md)
_*.md
_*.limpid

# Secrets / credentials — never commit
.env
.env.*
*.pem
*.key
*.p12
*.crt

# Audit receipts — local-only, never commit
.audit/
