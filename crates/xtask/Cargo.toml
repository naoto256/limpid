[package]
name = "xtask"
version = "0.0.0"
edition = "2024"
license.workspace = true
publish = false
description = "Dev tasks: snippet inventory generation, header lint, etc."

[[bin]]
name = "xtask"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
