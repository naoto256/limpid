//! `find_by` was removed in v0.7.5. Use `find(arr) { |x| x.key == value }`
//! with the new block-arg syntax instead. This file is kept as an empty
//! shim so a sloppy `git pull` doesn't leave a dangling reference; the
//! module is not registered anywhere.
